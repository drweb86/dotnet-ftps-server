<UserControl x:Class="FtpsServerWindows.MainMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:commands="clr-namespace:FtpsServerWindows.Commands"
             xmlns:res="clr-namespace:FtpsServerWindows.Resources">

    <UserControl.Resources>
        <commands:AboutCommand x:Key="AboutCommand"/>
        <commands:LicenseCommand x:Key="LicenseCommand"/>
        <commands:ThirdPartyNoticesCommand x:Key="ThirdPartyNoticesCommand"/>
        <commands:PrivacyCommand x:Key="PrivacyCommand"/>
        <commands:OpenLogsCommand x:Key="OpenLogsCommand"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="8,4" />
            <Style.Triggers>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
                </Trigger>
                <Trigger Property="Role" Value="TopLevelItem">
                    <Setter Property="Foreground" Value="{StaticResource WindowForegroundBrush}"/>
                </Trigger>
                <Trigger Property="Role" Value="SubmenuItem">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
                    <Setter Property="TextElement.Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
                </Trigger>
                <Trigger Property="Role" Value="SubmenuHeader">
                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
                    <Setter Property="TextElement.Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Menu DockPanel.Dock="Left"
              Background="Transparent"
              Margin="8"
              VerticalAlignment="Center">

        <MenuItem x:Name="AboutMenuItem"
              Header="{x:Static res:Strings.MenuHeader}"
              VerticalAlignment="Center">
            <MenuItem.Icon>
                <Image Source="../FtpsApp.png" />
            </MenuItem.Icon>
            <MenuItem.Items>
                <MenuItem

                    Header="{x:Static res:Strings.MenuWebSite}"
                    Command="{StaticResource AboutCommand}">
                    <MenuItem.Icon>
                        <Image Source="../FtpsApp.png" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static res:Strings.MenuLicense}" Command="{StaticResource LicenseCommand}" />
                <MenuItem Header="{x:Static res:Strings.MenuPrivacy}" Command="{StaticResource PrivacyCommand}" />
                <MenuItem Header="{x:Static res:Strings.MenuThirdPartyNotices}" Command="{StaticResource ThirdPartyNoticesCommand}" />
            </MenuItem.Items>
        </MenuItem>

        <MenuItem Header="{x:Static res:Strings.MenuLogs}"
                  FontWeight="Bold"
                  VerticalAlignment="Center"
                  Command="{StaticResource OpenLogsCommand}" />

        <MenuItem Header="{x:Static res:Strings.MenuStart}"
                  x:Name="PlayIcon"
                  Foreground="Green"
                  FontWeight="Bold"
                  VerticalAlignment="Center"
                  Click="StartStopButton_Click" />

        <MenuItem Header="{x:Static res:Strings.MenuStop}"
                  x:Name="StopIcon"
                  Click="StartStopButton_Click"
                  FontWeight="Bold"
                  Foreground="PaleVioletRed"
                  VerticalAlignment="Center"
                  Visibility="Collapsed" />
    </Menu>
</UserControl>
